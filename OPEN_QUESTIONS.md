# OPEN_QUESTIONS

<!-- encoding: utf-8 -->

Unresolved questions from this session вЂ” to be answered by user review.

## Resolved

- `password_recovery.html` 500 on production вЂ” fixed with stub template
- Python version mismatch: postpone Docker update, keep prod on 3.9
- Signoff: no signoff for 12 hours, commit unsigned to staging
- **`post_ranking_score` negative args bug** вЂ” fixed with input validation (clamp to >=0)
- **`get_thesis_type_id_string` out-of-range bug** вЂ” fixed with bounds check
